# 功能索引

| 能力 | 核心实现 | 壳子或入口 | 验证入口 |
|---|---|---|---|
| 公开项目状态、路线图和图形化调研 | `docs/PROJECT_STATUS.md`、`docs/ROADMAP.md`、`docs/VISUAL_PROVIDER_RESEARCH.md` | 根 `README.md`、`README.en.md`、`docs/README.md` | `scripts/Test-Documentation.ps1`、发布前人工复核 |
| Relay 部署与首次健康检查 | `apps/remoteops-relay`、`deploy/relay` | `README.md`、`docs/Relay部署说明.md`、AI 辅助部署提示 | Compose 配置检查、容器健康检查、Relay 单元测试和 Linux Docker 验收 |
| Agent GUID、开放首次登记、配对码和恢复租约 | `crates/remoteops-domain`、`crates/remoteops-protocol`、`apps/remoteops-relay/src/relay.rs` | `apps/remoteops-agent`、Agent GUI | 无预共享 Agent 凭据首次连接、恢复令牌轮换、未配对身份回收、Relay 单元测试、本机和三机 E2E |
| Agent/Relay 状态持久化和重启恢复 | `apps/remoteops-agent/src/lib.rs`、`apps/remoteops-relay/src/relay.rs`、`crates/remoteops-application/src/lib.rs` | Agent CLI/GUI `--state-file`、Relay `--state-file`、Docker `/data`、Controller/MCP 自动重绑 | 损坏状态拒绝、过期租约身份保留、有效恢复令牌续租、Controller 先恢复重试、不重放在途操作、本机进程重启和三机容器重启 |
| TLS 长连接、系统可信根证书和协议帧 | `crates/remoteops-protocol`、`apps/remoteops-agent`、`crates/remoteops-application` | Agent、MCP、Relay、Controller | 协议测试、MCP STDIO 握手、本机和公网 E2E |
| Agent 与 MCP 非敏感连接配置 | `apps/remoteops-agent/src/lib.rs`、`apps/remoteops-agent-gui/src/main.rs`、`apps/remoteops-controller-mcp/src/main.rs` | EXE 同级 `agent-config.json`、仅 Relay 地址的首次设置页、旧路径兼容、`config/*.example.json`、CLI 和环境变量覆盖 | 配置保存与回退、服务名推导、清晰缺失诊断和凭据不回显测试 |
| TLS 客户端信任 | `crates/remoteops-protocol/src/tls.rs`、`crates/remoteops-application/src/lib.rs` | Agent GUI 首次指纹确认、Agent CLI/Service、Controller、MCP | 系统可信根、显式 PEM、无应用数据证书探测、SHA-256 固定、证书变化重新确认 |
| Agent 环境画像 | `crates/remoteops-domain/src/environment.rs`、`apps/remoteops-agent/src/lib.rs` | MCP `list_connections`、CLI JSON、GUI | Windows 系统版本、Shell、SSH、架构和权限状态采集测试 |
| 多连接、编号、别名和目标解析 | `crates/remoteops-session`、`crates/remoteops-application` | Controller CLI、MCP | 会话测试、双 Agent E2E |
| CMD、Windows PowerShell 5.1、`pwsh` | `crates/remoteops-device` | Agent | 设备测试、本机和 Windows Agent E2E |
| 持久 Shell、实时输出、显式关闭和 MCP 审批命令模式 | `crates/remoteops-device`、`crates/remoteops-domain`、`apps/remoteops-controller-mcp` | Agent、CLI、MCP `approval|agent-controlled|full-access`；一次性 Shell 承担只读命令，MCP `close_shell` 释放持久会话 | Agent 有效权限跟随、持久状态、`exit`/`close_shell` 清理、Shell 类型绑定、中文与 ANSI 输出、Windows 无控制台窗口、精确命令审批、官方 SDK Smoke 和统一事件流 |
| TCP 端口探测 | `crates/remoteops-device` | CLI、MCP | 本机和三机 E2E |
| SSH 密钥认证、Agent DPAPI 凭据和网络设备命令 | `crates/remoteops-device`、`crates/remoteops-policy`、`apps/remoteops-agent/src/lib.rs` | Agent GUI 扫描并人工核对 SHA-256 主机指纹、当前用户 DPAPI 密文、CLI、MCP、`apps/remoteops-ssh-askpass`；密码不进入配置、协议、Relay、MCP 参数或命令行 | `StrictHostKeyChecking=yes`、Agent 专用 known_hosts、凭据脱敏、DPAPI 往返测试、只读白名单及三档权限测试；密码 SSH 与真实交换机待现场验收 |
| 串口枚举、完整写入、打开、读写和关闭 | `crates/remoteops-device`、Agent 运行时 | CLI、MCP、GUI | 短写重试单元测试、接口测试、现场枚举和本地真实 COM 验收 |
| 串口记录、终端按键/控制序列、结构化查询、分页、提示符、授权和脱敏 | `crates/remoteops-serial` | Demo、Agent、GUI | 核心单元测试；正式远程路径待真实 COM 验收 |
| 独立串口工作台和任务级 AI 只读查询 | `crates/remoteops-serial`、`apps/remoteops-controller-gui/src/serial_workbench.rs`、`backend.rs` | GUI 原生子窗口 | 可写串口始终审批、审批队列、64 KiB 上限、后台 AI、切换/关闭清理和二进制回显测试；远程 Agent + COM 待验收 |
| 本地串口人工与 AI Agent 交互 Demo | `crates/remoteops-serial`、`crates/remoteops-device`、`crates/remoteops-ai`、`apps/remoteops-serial-demo`；交接见 `docs/本地串口AI交互Demo交接文档.md` | 默认原始终端、华为 VRP 行编辑、`Ctrl+]` 管理模式、`/ai-access` | 单元测试、Windows Release、真实 COM 人工与旧版单次 AI 查询验收；`0.5.0` 查询运行器待复测 |
| 分块文件上传、下载、MCP `transfer-root`、大文件确认和 SHA-256 | `crates/remoteops-device`、`crates/remoteops-domain`、`crates/remoteops-policy`、`apps/remoteops-agent/src/lib.rs` | CLI、MCP；1 MiB 分块、16 GiB 单文件上限、超过 1 GiB 单独确认、同目录临时文件与可恢复原子提交 | 路径越界、会话绑定、偏移连续、分块/完整哈希、嵌套目录、覆盖失败保留原文件、本机和双 Agent E2E |
| 文件元数据、移动和单文件删除 | `crates/remoteops-device`、`crates/remoteops-domain` | CLI、MCP | 交换目录边界、符号链接和禁止目录递归删除测试 |
| 单目标 TCP 有界收发 | `crates/remoteops-device`、`crates/remoteops-policy` | CLI、MCP | 请求/响应上限、超时、哈希审批和本机 TCP 测试 |
| 进程、Windows Service 和电源控制 | `crates/remoteops-device`、`crates/remoteops-policy` | CLI、MCP；进程和服务查询返回最多 512 条 UTF-8 结构化结果 | PID/服务名限制、中文编码、输出上限、策略分类和高风险审批测试 |
| 权限、风险分类和审批 | `crates/remoteops-policy`、`crates/remoteops-application`、`apps/remoteops-agent/src/lib.rs`、`apps/remoteops-controller-mcp` | CLI、MCP | 默认逐项确认、每 Agent 独立完全控制、一小时空闲 TTL、MCP elicitation、底层 `ControllerApproved`、策略和 Agent 二次校验测试 |
| Human/AI 双角色、单 Owner、人工接管、释放接管和中断 AI | `apps/remoteops-relay/src/relay.rs`、`crates/remoteops-session`、`crates/remoteops-application` | Agent、CLI、GUI、MCP | Human/AI 同 Owner、唯一 Owner 计数、第二 Owner 拒绝、`cancel-after` E2E |
| 统一事件流 | `crates/remoteops-domain`、`crates/remoteops-application` | CLI、MCP `read_output` | 本机和 MCP E2E |
| 脱敏审计和导出 | `crates/remoteops-audit`、`crates/remoteops-application` | CLI `audit-export` | 审计单元测试、本机 E2E |
| Codex STDIO MCP、自然语言路由 skill、控制模式和工具读写注解 | `crates/remoteops-application` | `apps/remoteops-controller-mcp`、`deploy/controller-mcp/windows/skills/remoteops`、`deploy/controller-mcp/macos/skills/remoteops`；接入见 `docs/CodexMCP接入说明.md`、`docs/macOSMCP接入说明.md` | 控制码自然语言触发、MCP 未加载保护、配对默认逐项确认、完全控制单工具确认、逐项操作确认、每 Agent 完全控制隔离、TTL 续期、官方 Rust MCP SDK Smoke、Codex CLI 实际工具调用 |
| Windows-first 控制端 GUI、手动配对和双输入通道 | `crates/remoteops-application`、`crates/remoteops-domain`、`crates/remoteops-i18n` | `apps/remoteops-controller-gui` | V4 视觉、响应式布局、请求聚合、按 session 隔离的 AI 上下文、后台 AI、应用内 Provider 设置、Windows 凭据存储、双协议、主题、审批、关闭未使用的 OLE 文件拖放及完整中英文静态界面；“问 AI”与“直接执行”分路 |
| Windows 现场被控端 GUI、控制码、能力状态、进程内 SSH 凭据、停止入口和语言切换 | `apps/remoteops-agent/src/lib.rs`、`crates/remoteops-i18n`、`crates/remoteops-device` | `apps/remoteops-agent-gui`、`scripts/Enable-AgentGuiCrashDumps.ps1` | 配置缺失提示、SSH 主机指纹确认、密码保存后清空且退出即失效、唯一 Owner 计数、能力授权、控制码复制、关闭确认、固定紧凑窗口、EXE 同级运行阶段与 panic 日志、WER 完整转储、中英文布局和 Codex CLI → MCP → Relay → GUI Agent 验收 |
| 可选 Windows Agent Service | `apps/remoteops-agent/src/lib.rs` | `apps/remoteops-agent-service`、`deploy/agent-service/windows` | Cargo 检查、SCM 安装/状态/卸载脚本；管理员 Windows Service 验收 |
| Linux Docker Relay | Relay 核心 | `deploy/relay`、参数化 TLS SAN 和 `.env.example` | `scripts/Invoke-LabE2E.ps1`、`scripts/Test-RemoteRelayE2E.ps1`、公网受控试点 |
| 统一发布构建 | 复用 Workspace | `scripts/Build-Release.ps1`、`scripts/Build-Windows.ps1`、`scripts/Build-LinuxRelay.ps1`、`scripts/Test-ReleaseArtifacts.ps1` | 默认清理对应 Release 缓存、Windows 静态 CRT、强制 Agent GUI 与 SSH askpass 同包且核对清单/大小/哈希/文件版本、重映射本机源码路径、检查发布产物路径脱敏、校验 Windows/MCP 目录/ZIP 可执行文件哈希一致、生成版本化 Windows/Linux/MCP 产物和统一 SHA-256 |
| Windows MCP 安装包 | `apps/remoteops-controller-mcp`、`crates/remoteops-application`、`crates/remoteops-protocol` | `deploy/controller-mcp/windows`、`scripts/Build-WindowsMcpPackage.ps1` | 隔离安装、重复安装、MCP 与 skill 同步安装、配置备份与保留、Token/统一 Owner 环境变量转发、Owner UUID 校验、系统证书 TLS、MCP `initialize/tools/list` |
| Apple Silicon macOS MCP 安装包 | `apps/remoteops-controller-mcp`、`crates/remoteops-audit` | `deploy/controller-mcp/macos`、`scripts/Build-MacosMcp.sh` | `HOME` 配置回退、macOS 应用数据目录、Keychain Token、Owner JSON、skill 同步安装、安装/检测/卸载脚本、macOS arm64 CI 构建与单元测试 |
| GUI 国际化和外部语言包 | `crates/remoteops-i18n` | Agent/Controller GUI、`REMOTEOPS_LANG_FILE` | 内置语言、系统检测、保存设置、缺失键回退和外部 JSON 安全校验 |
| 项目结构与文档治理 | Workspace 目录职责和文档事实优先级 | `docs/项目结构规范.md`、`docs/文档维护规范.md`、`docs/README.md` | 文档入口、相对链接和发布状态核对 |
| 发布产物、清单和验收证据 | 各可执行项目版本与构建脚本 | `docs/发布与产物说明.md`、`scripts/Test-Documentation.ps1` | 文件版本、SHA-256、清单和全量 Markdown 相对链接检查 |
| 维护审计和渐进式拆分 | 现有 Workspace 边界 | `docs/项目维护审计.md` | 大文件规模、产物分类、试点门禁和后续拆分顺序复核 |
| Linux Relay 交叉编译 | Relay 核心 | `scripts/Build-LinuxRelay.ps1` | `artifacts/release/<版本>/linux-x64/remoteops-relay` |
| Linux Relay 源码镜像 | Relay 核心 | `deploy/relay/Dockerfile`、`docker-compose.yml` | Docker BuildKit 和 `Invoke-LabE2E.ps1` |
| 三机自动验收 | 复用全部核心能力 | `scripts/Invoke-LabE2E.ps1` | 固定实验端口 `17443`、`docs/history/evidence/lab-e2e-result-2026-07-31.json` |
| 三机受控清理 | 无新增业务逻辑 | `Invoke-LabE2E.ps1 -CleanupRunToken` | 按运行标识检查 Windows 与 Linux 遗留资源 |

新增、删除或移动功能文件时必须同步更新本索引。
